<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="row">
      <div class="input-field col s8">
        <%= form.text_area :text ,id: "textarea1", class: "materialize-textarea", placeholder: "type a text"%>
      </div>
    </div>
  </div>

  <div class="actions waves-effect waves-light btn lime">
    <%= form.submit %>
  </div>

<% end %>
